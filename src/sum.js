const sum = () => {
};

export default sum;

// const nums = [a, b, ...others]
//     .map((n) => parseInt(n, 10))
//     .filter((n) => !Number.isNaN(n));
//   return nums.reduce((total, n) => total + n, 0);
